{
    "buildCommand": "npm run vercel-build",
    "outputDirectory": "web-build",
    "framework": "expo",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ]
  }